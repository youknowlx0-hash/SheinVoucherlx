providers = ["python"]

[phases.setup]
nixPkgs = ["python310"]

[phases.install]
cmds = ["python -m pip install --upgrade pip",
        "pip install -r requirements.txt"]

[start]
cmd = "gunicorn bot:app"
